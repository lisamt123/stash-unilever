/****************************************************************************
 *@Description:This script is used for on hover functionality for Image upload
 *@Author: Cognizant
 *@Created Date: 28/05/2015 
****************************************************************************/
/* Below function calls another function 'deletImage' which does the image delete functionality. */
function delImage(conId, secId, imgId, channelType) {
    deletImage(conId, secId, imgId, channelType);
}

/* Below function calls another function 'deleteImg' which does the concept image delete functionality. */
function delConceptImage(secConId) {
    deleteImg(secConId);
}