public class SetupHistory {

    //Use this for field changes
    //select id, Action, Section, display from SetupAuditTrail where action like '%Apex%' order by createdDate DESC
    //
    //
    
    //Workflow
    //Process Builder
    
    //select id, name from ProcessDefinition
    
    //Aura
    //select id, DeveloperName from AuraDefinitionBundle
    //Apex Classes
    //select id, name, status, LastModifiedDate from APEXCLASS where LastModifiedDate =THIS_WEEK
}