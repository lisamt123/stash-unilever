<apex:page standardController="Core_Approval_Header__c" extensions="Core_CA_MiniPageView_Override_Controller"> 
  <script type = "text/javascript"> 
       // For mobile
        if( (typeof sforce != 'undefined') && (sforce != null) ) {    
            sforce.one.navigateToURL('{!customPageURL}');
        }
        // For Desktop browser
        else{
             win = window.open('{!customPageURL}','_parent');
         }
  </script>          
</apex:page>