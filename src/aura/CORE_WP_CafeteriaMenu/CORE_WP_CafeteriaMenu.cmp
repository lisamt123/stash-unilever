<aura:component controller="Core_WP_WorkPlaceController">
	<aura:handler name="init" value="{!this}" action="{!c.doInit}" />
    <aura:attribute name="workplaceLocationId" type="String"/>
    <aura:attribute name="noDataErrorMessage" type="Boolean" default="false"/>
    <!--make the richtext information as responsive -->
    <style>
        .responsiveRichText img {
            height: 100% !important;
            width: 100% !important;
        }
    </style>
    <div class="topBarStyle">
        <div onclick="{!c.gotoPreviousPage}" class="topBackBar">
            <img src="/resource/NewsCenterIcons/back.svg" class="topBackBarIcon"></img>
        </div>
    </div>
    <div class="allPadding">    
        <img src="/resource/CORE_WP_WorkplaceStyles/Icons/CafeteriaMenu.svg" class="smallIcons"/>
        <div class="allPadding pageTitle">Cafeteria menu</div><br/>
        <!--<div class="menuDetailPannel"> class="responsiveRichText"
            <img src="/resource/CORE_WP_WorkplaceStyles/Icons/add.svg" onclick="{!c.increseMenuDetail}" class="smallIcons"/>
            <img src="/resource/CORE_WP_WorkplaceStyles/Icons/remove.svg" onclick="{!c.decreaseMenuDetail}" class="smallIcons"/>
            <img src="{!v.cafeteriaMenuDetail}" id="menuDetail"></img>
            <aura:if isTrue="{!v.noDataErrorMessage}">
                There is no data available currently to display for this section.
            </aura:if> 
        </div>-->

		<div class="responsiveRichText">
			<img src="{!v.cafeteriaMenuDetail}" id="menuDetail" onclick="{!c.SaveToDisk}"></img>
            <aura:if isTrue="{!v.noDataErrorMessage}">
                There is no data available currently to display for this section.
            </aura:if>
        </div>
    </div>
    
</aura:component>